# task1: UI 仕様確定とテスト観点整理

## 担当

developer

## 状態

- [x] 完了

## 作業内容

- 現在の `CalendarWidget._flexibleRowWeek` のセル構造を確認する。
- セル内に表示するゴミ名の上限を3件とする仕様を確定する。
- 4件以上の場合の `...+{超過数}` 表示位置とスタイルを決める。
- 対象ゴミが0件の日付セルをタップした場合に「出せるゴミはありません」を表示する仕様を整理する。
- ダイアログタイトルに表示する対象年月日を `YYYY年MM月DD日` 形式で算出する方針を決める。
- ダイアログに表示する本文、閉じる操作、テスト用 Key/Semantics を決める。

## 完了条件

- UI 仕様が実装可能な粒度で整理されている。
- 前月/翌月の日付セルを含め、ダイアログタイトルの日付算出方針が整理されている。
- 既存テストへの影響箇所が洗い出されている。
- task2 以降で迷わず実装できる判断が残っている。

## 期待成果物

- 本ファイルの状態更新。
- 必要に応じた `issues/006/plan.md` の更新。

## 実施結果

- セル内表示は先頭3件、超過時は `...+{超過数}` とする仕様で確定した。
- 対象ゴミが0件の日付セルもタップ時にダイアログを開き、「出せるゴミはありません」を表示する仕様で確定した。
- ダイアログタイトルは対象実日付を `YYYY年MM月DD日` 形式で表示する。
- 実日付は表示ページの年月、週番号、セルの日付から算出する。
