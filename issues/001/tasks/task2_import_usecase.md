# task2: 取り込み UseCase/Repository 実装

## 担当

developer

## 内容

- 既存データ全削除と API 結果登録を一連の処理として実装する。
- 反映成功時にリモート同期待ち状態へ更新する。
- 初回ダイアログ表示状態と編集画面表示メッセージを保存する Repository を追加または既存 Repository に統合する。
- 取り込み結果をローカル通知へ渡す Service を追加する。
- 正常レスポンス反映後は、次に編集画面を開いた際に取り込み完了メッセージを一度だけ表示する。
- エラー時は、次に編集画面を開いた際にエラー内容を一度だけ表示する。

## テスト

- 成功時に既存データが置き換わる。
- 反映後に同期状態が `SYNCING` になる。
- API エラー時に既存データが削除されない。
- 初回表示状態が実行・キャンセルどちらでも更新される。
- 編集画面メッセージが保存・取得・表示後削除できる。

## 完了条件

- 対象単体テストが通る。
- 失敗時のデータ破壊がない。
